name: pet_save
description: "PetSave App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.3.0

  # Persistência remota – SDK oficial Supabase (RQ02)
  supabase_flutter: ^2.5.6

  # Recurso nativo – câmera/galeria (RQ04)
  image_picker: ^0.8.7+5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
